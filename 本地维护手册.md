# 本地维护手册（不提交到 git）

> 本文仅存于本机。已通过 `.git/info/exclude` 排除，`git status` 不显示、不会被推送。
> 更新时间：2026-09-01 · 当前版本 0.3.0 · npm 已发布 0.3.0

---

## 1. 当前收录状态台账

| 项目 | 值 |
|------|-----|
| GitHub 仓库 | https://github.com/162568316/dsh-tokenrhythm-bill |
| npm 包 | `dsh-tokenrhythm-bill`（0.3.0 已发布，`repository` 已回指仓库 → 自动 repo-verified） |
| 目录注册表 | `awesome-dsh-plugin/awesome-dsh-plugin`（收录 PR：https://github.com/awesome-dsh-plugin/awesome-dsh-plugin/pull/4035 ） |
| **收录状态** | **✅ 已合并（2026-09-01 12:10 by fkysly）且目录当日已上线**：https://awesome-dsh-plugin.com/p/162568316/dsh-tokenrhythm-bill/ |
| npm 映射 | 目录里 `npm/downloads` 暂为 null —— registry 自动同步一两天内补上，届时安装走秒级 npm 路线 |
| 目录条目文件 | 注册表仓库内 `data/plugins/162568316__dsh-tokenrhythm-bill.yml` |
| 分类 | `usage`（用量与计费；发布目录里数组分类无先例，用的单分类） |
| 仓库 topics | `dsh-plugin` `deepseek-harness` `dsh` |
| 我的 fork | https://github.com/162568316/awesome-dsh-plugin （以后改条目描述从这里提 PR） |
| 截图声明 | 仓库根 `screenshots.json` → `image/{入口,模型,余额,设置}.png` |

**市场可见路径**：~~PR #4035 合并 → 次日凌晨目录 CI 刷新~~ **已完成（合并当天目录即刷新）**：
dsh-market（设置 → Plugin Market）/ awesome-dsh-plugin.com / dshmarket.com / DSH Get 同步可查。

---

## 2. 以后维护要做什么

### 换截图 / 改图 —— 不需要任何 PR

1. 更新 `image/` 里的图片（保持或新增，1–8 张）；
2. 按需改根目录 `screenshots.json` 的顺序与清单（相对路径，不能出插件目录）；
3. `git add image screenshots.json && git commit && git push` 即可。
   下一次夜间构建自动生效，市场卡片就会用新图。

规则：图片要么是仓库内相对路径，要么是 GitHub 托管 https 绝对 URL
（raw/user-images/camo/github 附件），第三方图床会被拒。

### 发新版本（0.4.0…）—— 不需要动注册表

1. `package.json` 改 `version` → 跑 `tests`（node --test，应全绿）；
2. `npm publish`：本机 `~/.npmrc` 已登录（littleali）。
   发布会触发 2FA：弹出浏览器用安全钥匙「滴滴滴」确认，或窗口提示时输入
   验证器 6 位码（验证码绝不进聊天）；
3. `npm view dsh-tokenrhythm-bill version` 验证。
   目录条目无需任何动作：npm↔仓库映射由注册表 CI 从 registry 自动采集。

### 改目录条目描述 / 分类 —— 需要一个小 PR

条目 yml 的文字改动要向 `awesome-dsh-plugin/awesome-dsh-plugin` 提 PR
（官方：修正描述的 PR 同样欢迎）。流程：

1. fork 已在：`162568316/awesome-dsh-plugin`，从它建分支；
2. 改 `data/plugins/162568316__dsh-tokenrhythm-bill.yml`；
3. push 后向注册表仓库开 PR（引用 #4035 的做法）。

**yml 规范速查**：
- 只有 `url` / `name`（owner/repo）/ `category` / `description.{en,zh}` 五个字段；
  `npm:` 手写会被校验拒绝（自动采集）；`page/stars/downloads/added` 是 CI 生成的；
- en 必填，zh 可选但务必给；描述必须与代码逐句相符（维护者会读源码核对）；
- 描述含半角 `: ` 必须用单引号包住，串内 `'` 写成 `''`；中文全角冒号无此问题；
- 一个 PR 最多 3 条。

---

## 3. 投稿脚本与日志（可丢，%TEMP% 会被系统清理）

| 文件 | 用途 |
|------|------|
| `%TEMP%\dsh-awesome-pr-submit.ps1` | 自动投稿脚本：自检门槛（≥1 天 + ≥10 提交）→ fork → API 提交条目 → 开 PR。幂等，可重复跑 |
| `%TEMP%\dsh-awesome-entry.yml` | 投稿条目内容（脚本从这里读，UTF-8） |
| `%TEMP%\dsh-awesome-pr.log` | 投稿日志 |

丢了也不心疼：条目内容 = 本仓库 `MARKET-SUBMISSION.md` 里的 description
转成 yml（去掉 `npm`/`install` 字段、`name` 写成 `owner/repo`），脚本逻辑
见 git 历史里本文档的快照。

⚠️ 凭据边界：脚本通过 `git credential fill` 取本机已存凭据，只在进程内存
使用，不落盘、不打印；npm 凭据在 `~/.npmrc`；两处都不进聊天。

---

## 4. 已知待办 / 提醒

- [x] ~~PR #4035 等 CI 与维护者评审~~ → **2026-09-01 12:10 已合并，目录当日上线**；
- [x] ~~中国区市场镜像包更新~~ → 探针盯 `dsh-plugin-catalog` 新版（当前 2026.831.3071 不含本插件；新版发布后市场即搜到，npm 映射同步补上）；
- [ ] `git status` 里可能出现未跟踪文件，属正常（本文档与临时脚本都已本地排除/不提交）；
- [ ] 恢复码泄露事故（2026-08-31 两次贴入聊天）——若还没重新生成，尽快去
      npm → 账号设置 → 两步验证 → Manage Recovery Codes → Generate new
      recovery codes，Download 存本地。

---

## 5. 插件安装排障（本机踩过的坑）

**现象**：`dsh plugin add` 卡在 `downloaded 0, added 0`，或报
`EPERM: rename ..._tmp_xxx -> dsh-tokenrhythm-bill`。

**原因**：旧的 junction（本地链接安装）被进程占用 / 残留 `_tmp_*` 目录没清，
pnpm 换装新目录时改不了名。

**处理顺序**（2026-09-01 实测有效）：

```powershell
# 1. 清残留临时目录
Get-ChildItem C:\Users\22366\.dsh\profiles\desktop\node_modules -Filter 'dsh-tokenrhythm-bill_tmp_*' -Directory | Remove-Item -Recurse -Force

# 2. 只删链接不删源（cmd rmdir 对 junction 只移除链接本身）
cmd /c rmdir "C:\Users\22366\.dsh\profiles\desktop\node_modules\dsh-tokenrhythm-bill"

# 3. 重新安装（npm 路线，走 npmmirror 秒装；github: 路线在本机网络下易卡）
dsh plugin add dsh-tokenrhythm-bill
```

**两种安装方式**：

| 命令 | 效果 |
|------|------|
| `dsh plugin add dsh-tokenrhythm-bill` | npm 固定版本副本（当前是 0.3.0），日常使用 |
| `dsh plugin add E:\dsh-插件\dsh-tokenrhythm-bill` | 本地链接 junction，改代码即时生效，开发调试用 |

⚠️ 2026-09-01 起本机已从「本地链接」切成「npm 0.3.0 副本」——改 E 盘源码
面板不会变；回到开发模式用第二条命令切回。GitHub 源码路线
（`add github:...`）在本机经常卡下载，不推荐。
